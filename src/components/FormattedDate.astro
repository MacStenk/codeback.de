---
const { date } = Astro.props;
// Konvertiere zu Date-Objekt falls String
const dateObj = date instanceof Date ? date : new Date(date);
---

<time datetime={dateObj.toISOString()}>
  {
    dateObj.toLocaleDateString("de-DE", {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  }
</time>
