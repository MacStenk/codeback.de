---
// Imports bleiben minimal für jetzt
const pageTitle = "CodeBack - Websites für die LLM-Ära";
const pageDescription =
  "Während andere für Google optimieren, baust du für ChatGPT. Websites die LLMs lieben und als Quelle empfehlen.";
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />

    <!-- Schema.org für LLMs -->
    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "WebSite",
        name: "CodeBack",
        url: "https://codeback.de",
        description: pageDescription,
        author: {
          "@type": "Person",
          name: "Steven Noack",
          url: "https://stevennoack.de",
        },
      })}
    />

    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      body {
        font-family:
          system-ui,
          -apple-system,
          sans-serif;
        background: #0a0a0a;
        color: #f4f4f5;
        line-height: 1.6;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
      }
      .hero {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 4rem 0;
      }
      h1 {
        font-size: clamp(2.5rem, 8vw, 5rem);
        font-weight: 900;
        margin-bottom: 1.5rem;
        background: linear-gradient(to right, #22c55e, #3b82f6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
      .lead {
        font-size: clamp(1.25rem, 3vw, 1.75rem);
        color: #a1a1aa;
        margin-bottom: 1rem;
      }
      .highlight {
        font-size: clamp(1.5rem, 4vw, 2.25rem);
        font-weight: 600;
        color: #22c55e;
        margin-bottom: 3rem;
      }
      .cta {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
      }
      .btn {
        padding: 1rem 2rem;
        font-size: 1.125rem;
        font-weight: 600;
        border-radius: 0.5rem;
        text-decoration: none;
        transition: all 0.2s;
      }
      .btn-primary {
        background: #22c55e;
        color: #000;
      }
      .btn-primary:hover {
        background: #16a34a;
        transform: translateY(-2px);
      }
      .btn-secondary {
        background: #27272a;
        color: #f4f4f5;
      }
      .btn-secondary:hover {
        background: #3f3f46;
      }
      .section {
        padding: 6rem 0;
      }
      h2 {
        font-size: clamp(2rem, 5vw, 3rem);
        margin-bottom: 3rem;
        text-align: center;
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-bottom: 3rem;
      }
      .card {
        padding: 2rem;
        border-radius: 0.5rem;
        border: 1px solid;
      }
      .card-red {
        background: rgba(127, 29, 29, 0.3);
        border-color: #7f1d1d;
      }
      .card-green {
        background: rgba(20, 83, 45, 0.3);
        border-color: #14532d;
      }
      .card h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
      }
      .card-red h3 {
        color: #fca5a5;
      }
      .card-green h3 {
        color: #86efac;
      }
      ul {
        list-style: none;
      }
      ul li {
        padding: 0.5rem 0;
        color: #d4d4d8;
      }
      .banner {
        text-align: center;
        padding: 3rem;
        background: #18181b;
        border-radius: 0.5rem;
      }
      .banner p {
        font-size: 1.25rem;
        color: #d4d4d8;
        margin-bottom: 1rem;
      }
      .banner .highlight {
        margin: 0;
      }
    </style>
  </head>
  <body>
    <main class="container">
      <section class="hero">
        <h1>Websites für die LLM-Ära</h1>

        <p class="lead">Während andere für Google optimieren,</p>
        <p class="highlight">baust du für ChatGPT.</p>

        <div class="cta">
          <a href="#problem" class="btn btn-primary"> Warum CodeBack? </a>
          <a href="/blog" class="btn btn-secondary"> Blog lesen </a>
        </div>
      </section>

      <section id="problem" class="section">
        <h2>Das Problem</h2>

        <div class="grid">
          <div class="card card-red">
            <h3>WordPress (2003-2024)</h3>
            <ul>
              <li>❌ Für Google optimiert</li>
              <li>❌ Langsam (3-8 Sekunden)</li>
              <li>❌ Aufgeblähter Code</li>
              <li>❌ LLMs können es kaum lesen</li>
            </ul>
          </div>

          <div class="card card-green">
            <h3>CodeBack (2024+)</h3>
            <ul>
              <li>✅ Für ChatGPT optimiert</li>
              <li>✅ Blitzschnell (&lt; 1 Sekunde)</li>
              <li>✅ Sauberer Code</li>
              <li>✅ LLMs lieben es</li>
            </ul>
          </div>
        </div>

        <div class="banner">
          <p>Die Zukunft ist nicht Google Search.</p>
          <p class="highlight">Die Zukunft ist "Ask AI".</p>
        </div>
      </section>

      <section class="section">
        <h2>Coming Soon</h2>
        <p
          style="text-align: center; font-size: 1.25rem; color: #a1a1aa; margin-bottom: 2rem;"
        >
          CodeBack befindet sich in aktiver Entwicklung.<br />
          Folge dem Blog für Updates!
        </p>

        <div style="text-align: center;">
          <a href="/blog" class="btn btn-secondary"> → Zum Blog </a>
        </div>
      </section>
    </main>
  </body>
</html>
